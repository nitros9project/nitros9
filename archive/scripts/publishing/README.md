# Historical publishing tool

`burst` copied generated web content into the project's former SourceForge
web directory. That server-specific workflow is no longer used and the script
is retained only as project history.
